Info3: <<
Package: libspatialite2-shlibs
Version: 3.0.1
Revision: 10
Description: Shared libraries for libspatialite2 package
License: OSI-Approved
Homepage: http://www.gaia-gis.it/spatialite/
Maintainer: BABA Yoshihiko <babayoshihiko@mac.com>
DescDetail: <<
The SpatiaLite extension enables SQLite to support 
spatial data too [aka GEOMETRY], in a way conformant 
to OpenGIS specifications.
<<

Depends: <<
  libfreexl1-shlibs (>=1.0.2-1),
  libgeos3.6.1-shlibs,
  libiconv,
  libproj9-shlibs,
  sqlite3-shlibs
<<
BuildDepends: <<
  fink (>= 0.28),
  fink-package-precedence,
  flag-sort,
  libfreexl1-dev (>=1.0.2-1),
  libgeos3.6.1,
  libiconv-dev,
  libproj9,
  sqlite3-dev
<<
Conflicts: libspatialite2 (<< 3.0.1-10)
Replaces: libspatialite2 (<< 3.0.1-10)

# Unpack Phase.

Source: http://www.gaia-gis.it/gaia-sins/libspatialite-sources/libspatialite-%v.tar.gz
Source-MD5: 450d1a0d9da1bd9f770b7db3f2509f69

SetCPPFLAGS: -Os -I%p/opt/libgeos3.6.1/include
SetLDFLAGS: -L%p/opt/libgeos3.6.1/lib
# Need to keep local -I first (avoids needing fragile BuildConflicts)
ConfigureParams: <<
	--target=macosx \
	CC="flag-sort gcc" \
	--disable-static \
	--enable-dependency-tracking
<<

PatchScript: <<
  # Patch configure not to link like Puma on Yosemite
  perl -pi -e 's/(10\.\[012\])\*/\1\,.\*/' configure

  perl -pi -e "s|SUBDIRS = src test examples|SUBDIRS = src test|" Makefile.in

  perl -pi -e 's/(-version-info.*)/\1 -no-undefined/ unless /-no-undefined/' src/Makefile.in
<<

CompileScript: <<
#!/bin/sh -xe

  # Set up Libraries
  ./configure %c
  
  make
  fink-package-precedence --prohibit-bdep=libspatialite2 .
<<

# Install Phase.
InstallScript: <<
#!/bin/sh -xe
  # Install Libraries
  make install prefix=%i

  rm -rf %i/include
  rm %i/lib/libspatialite.{dylib,la}
  rm -rf %i/lib/pkgconfig
<<
Shlibs: <<
  %p/lib/libspatialite.2.dylib  5.0.0 %n (>= 3.0.1-1)
<<
DocFiles: AUTHORS COPYING INSTALL README

# End of Info3
<<
